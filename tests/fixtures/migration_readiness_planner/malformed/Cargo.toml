[workspace]
members = []
resolver = "3"

[workspace.metadata.asupersync-test-fixture]
purpose = "The contract test generates the intentionally malformed Cargo.toml in a temporary directory so Git consumers never encounter invalid tracked manifests."
