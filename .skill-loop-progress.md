# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: non-ATP runtime spawn admission and completion path
# Total Passes: 10
# Started: 2026-07-12T04:51:00Z

## Status: IN PROGRESS — Pass 5 of 10 complete; Pass 6 ready

## Preconditions
- Tracker integrity restored in `07cd7cecc` (`br-asupersync-kq9qn9`).
- Campaign tracked by `asupersync-spawn-admission-opt-gauntlet-p8zm1v`.
- Corrected spawn benchmark landed under `br-asupersync-3j33bk`: persistent producers and a condvar completion latch replace timed thread lifecycle and spin polling; the remote release-perf build and all six liveness cells passed.
- Every Cargo benchmark, profile, and proof must use strict remote RCH with no local fallback.
- ATP paths are out of scope.

## Missions
1. Reproducible baseline: capture same-worker direct/mailbox p50, tails, throughput, memory, toolchain, and host fingerprint with the corrected harness.
2. Profile attribution: capture a CPU profile or a documented non-perf fallback and rank the top five spawn-path costs by evidence.
3. Admission-mode lever: evaluate the existing mailbox/flat-combining path as the default using a one-change A/B and full ordering/cancellation proof.
4. Drain-buffer reuse: remove measured per-batch allocation only if allocation attribution clears the opportunity-score gate.
5. Batch-size sensitivity: measure 1/4/16/64 admission batches and change one constant or policy only when tails and throughput both justify it.
6. State-lock critical section: isolate and shorten the measured admission lock hold while preserving task/region/obligation atomicity.
7. Task and join allocation traffic: reduce the leading measured Arc/box/record allocation without changing panic, cancellation, or result semantics.
8. Ready-publication locality: evaluate batched ready injection or owner-local publication using deterministic FIFO and fairness gates.
9. Adversarial tails: optimize the leading p95/p99 cost under cancel storms, quota denial, region close, and producer overdrive without unbounded queues.
10. Convergence pass: re-profile, compare against Pass 1, run golden and broad proof lanes, and stop or name the next evidence-backed frontier.

## Completed Passes

### Pass 1 — corrected same-worker baseline

- Fresh agent: `extreme_pass1_baseline`; completed 2026-07-12 without repository edits.
- Source: `c70e89d0a5b9b7c68ecc43d71ff3bb55aca03a66`; benchmark artifact SHA-256 `93fa34b0783d53fed48d1d29a1f72295effbf961b04411a8c9e2c695ccf58c2d`.
- Worker: `vmi1152480`, Ubuntu 25.10, Linux 6.17.0-40-generic, KVM, 10-vCPU AMD EPYC, 61.7 GiB RAM, `rustc 1.98.0-nightly (c397dae80 2026-07-02)`.
- Preserved remote evidence: `/root/perf-baselines/asupersync-spawn-pass1-final-c70e89d0a-20260712T060643Z`.

| Corrected cell | Criterion p50 ms (95% CI) | Throughput Kelem/s | Raw p95 ms | Raw p99 ms | Max RSS KiB |
|---|---:|---:|---:|---:|---:|
| single/direct | 42.029 (35.745–51.273) | 22.710 | 62.932 | 70.824 | 110,344 |
| single/mailbox | 25.400 (20.857–28.137) | 39.316 | 34.087 | 35.067 | 113,904 |
| 4P/direct | 37.520 (32.733–44.360) | 27.524 | 50.761 | 55.629 | 111,280 |
| 4P/mailbox | 24.411 (18.849–30.004) | 36.370 | 37.785 | 52.795 | 114,376 |
| 8P/direct | 36.547 (34.203–42.786) | 26.761 | 47.919 | 52.728 | 111,416 |
| 8P/mailbox | 23.725 (20.689–28.061) | 34.537 | 36.531 | 42.988 | 114,736 |

- Method: one bounded Criterion run per cell, 3 s warmup, nominal 10 s measurement, 20 raw linear samples (the benchmark group overrides the requested 100), and 100,000 resamples. Raw p95/p99 are nearest-rank percentiles of `times[i] / iters[i]`; with 20 samples p99 is the maximum. RSS is whole benchmark plus Criterion, not runtime-only heap.
- Independent verification: re-read all six `estimates.json`, `sample.json`, logs, `/usr/bin/time -v` receipts, fingerprint, load, commands, and artifact hash from the evidence root; recomputed raw percentiles with `jq`.
- Limitation/verdict: host load rose from 9.13 to 10.96 on 10 vCPUs and slope R² ranged from 0.035 to 0.59. Mailbox was lower in this snapshot, but this is not durable default-flip evidence. Pass 2 must attribute direct and mailbox costs separately under a fixed-duration profile, preferably during a lower-load window.

### Pass 2 — sustained direct/mailbox CPU attribution

- Fresh agent: `extreme_pass2_profile`; completed 2026-07-12 without repository edits.
- Source/artifact: same `c70e89d0a` artifact and SHA-256 as Pass 1.
- Preserved remote evidence: `/root/perf-baselines/asupersync-spawn-pass2-c70e89d0a-20260712T061700Z` (154 MiB, 39-entry checksum manifest verified).
- Valid profile shape: exact corrected 8P cell, Criterion `--bench ... --profile-time 15`, `perf record -F 499 -e cycles:u -g --call-graph dwarf,8192`, 45 s hard timeout. Direct captured 8,822 samples/~16.663B user-cycle events over 17.289 s; mailbox captured 8,924/~16.399B over 17.826 s; both lost zero samples.

| Rank | Direct resolved self-cycle cost | Share |
|---:|---|---:|
| 1 | `std::sys::sync::mutex::futex::Mutex::lock_contended` | 5.20% |
| 2 | `RuntimeState::task_completed` | 3.83% |
| 3 | vDSO `clock_gettime` via `Timespec::now` / `TimerDriver::now` | 3.46% |
| 4 | `RuntimeState::create_task_infrastructure<()>` | 3.37% |
| 5 | `EpochConsistencyTracker::notify_epoch_transition` | 3.23% |

| Rank | Mailbox resolved self-cycle cost | Share |
|---:|---|---:|
| 1 | `RuntimeState::task_completed` | 5.00% |
| 2 | vDSO `clock_gettime` via `Timespec::now` / `TimerDriver::now` | 4.08% |
| 3 | `RuntimeState::validate_task_protocol_transition` | 4.00% |
| 4 | `EpochConsistencyTracker::notify_epoch_transition` | 3.86% |
| 5 | `CancelProtocolValidator::validate_task_transition` | 3.05% |

- Mechanism evidence: resolved `lock_contended` self cycles fell from 5.20% direct (3.63% worker plus 1.58% producer/injection) to 0.63% mailbox. Mailbox admission measured `admit_spawn_request` at 8.69% inclusive/0.90% self, `admit_spawn_record` at 4.64%/2.04%, and producer `SpawnMailbox::enqueue` at 2.91%/0.87%. CPU attribution shifted from 56.71% worker / 43.25% producer in direct to 93.41% / 6.59% in mailbox.
- Independent verification: checked both perf headers, zero-loss/sample windows/event counts, flat and inclusive symbol reports, per-command shares, vDSO clock call chains, load receipts, artifact fingerprint, and `sha256sum -c` for all 39 evidence entries.
- Limitations: only user cycles were recorded; roughly 30–32% remained in stripped libc and about 2.2% unknown. No IPC, allocation-count, syscall-count, kernel-cycle, or lock-wait-duration claim is supported. Load rose 10.13→11.51 direct and 11.98→13.21 mailbox on 10 vCPUs, so sequential total-cycle comparison is inadmissible.
- Pass 3 selection: evaluate the existing mailbox path as the default in an isolated, revertible one-change A/B (opportunity score `4 impact × 3 confidence ÷ 2 effort = 6.0`). This selects the experiment only; a default flip requires lower-load repeated performance evidence plus ordering, cancellation, shutdown, region-close, quota, panic/join, leak, wake/drain, backlog, and park-race proofs.

### Pass 3 — lower-load admission-mode A/B, rejected fail-closed

- Fresh agent: `extreme_pass3_default_ab`; completed 2026-07-12 with no retained repository source change.
- Source/artifact: `c70e89d0a`, SHA-256 `93fa34b0783d53fed48d1d29a1f72295effbf961b04411a8c9e2c695ccf58c2d`.
- Worker: `hz2`, Linux 7.0.0-15-generic, 16-vCPU AMD EPYC Genoa, 31,326 MiB RAM. Initial load was 0.11/0.16/0.09; one-minute load receipts stayed between 0.24 and 1.83 during the experiment.
- Preserved evidence: remote `/root/perf-baselines/asupersync-spawn-pass3-c70e89d0a-20260712TtIjVI2`; independently copied and verified locally at `/data/tmp/asupersync-spawn-pass3-c70e89d0a-20260712TtIjVI2`.
- Predeclared performance gate: mailbox raw p50 at least 10% lower in both order-reversed pairs for every cell, no mailbox p95/p99 more than 10% above adjacent direct, and every command exit zero on one lower-load worker.

| Corrected cell and order pair | Direct p50 ms | Mailbox p50 ms | Mailbox change | Direct / mailbox p95 ms | Direct / mailbox p99 ms |
|---|---:|---:|---:|---:|---:|
| single D1/M1 | 19.788 | 10.843 | -45.2% | 24.598 / 15.321 | 28.035 / 15.840 |
| single M2/D2 | 19.675 | 10.287 | -47.7% | 25.901 / 13.646 | 26.305 / 15.204 |
| 4P D1/M1 | 25.714 | 11.623 | -54.8% | 29.896 / 15.632 | 31.419 / 16.423 |
| 4P M2/D2 | 21.670 | 9.545 | -56.0% | 27.127 / 15.056 | 28.460 / 15.545 |
| 8P D1/M1 | 25.857 | 11.753 | -54.5% | 28.061 / 14.488 | 28.259 / 14.590 |
| 8P M2/D2 | 27.351 | 10.766 | -60.6% | 29.541 / 11.933 | 29.740 / 12.824 |

- Method: alternating D/M then M/D for single, 4P, and 8P; 12 commands, all exit zero; 2 s warmup, nominal 5 s measurement, 20 raw linear samples per run, and a 55 s hard timeout. Raw percentiles use `times[i] / iters[i]`; p99 is the maximum of 20 samples.
- Independent verification: re-read `decision.md`, all twelve `raw_percentiles.jsonl` rows, the exact proof failure receipt, worker/order/load receipts, and the source/artifact fingerprint; `sha256sum -c SHA256SUMS` passed for the complete local evidence copy.
- Performance verdict: the predeclared performance gate passed in every pair and every mailbox tail was lower. This supports only these explicit-mode corrected benchmark cells for this artifact and host; it does not establish a safe default.
- Correctness gate: the proposed isolated `RuntimeConfig::default()` marker/docs/test change passed pinned rustfmt and `git diff --check`, but strict RCH compilation failed before test execution because `mutex_contention_under_lab_runtime` held the newly `!Send` borrowed `MutexGuard` across `yield_now().await` in a `Send`-required task (`E0277`, worker `vmi1152480`, exit 101 after 1,280,273 ms).
- Decision: reject/defer the default flip. The candidate `src/runtime/config.rs` diff was manually reverted with `apply_patch`; `git diff --exit-code -- src/runtime/config.rs` is clean. No check, Clippy, behavioral-equivalence, fairness, cancellation, memory, broad-health, or production-tail claim is made.
- Prerequisite before the next optimization pass: repair and remotely prove the Mutex conformance test without weakening the borrowed guard's same-thread invariant, then choose the next fresh-agent mission from the verified frontier. A benchmark win alone never authorizes the default flip.

### Between Pass 3 and Pass 4 — correctness prerequisite repaired

- Repair commit: `c44d7736c` (`br-asupersync-kd89v4`), pushed to `origin/main` and mirrored to `origin/master`.
- Preserved the borrowed `MutexGuard` `!Send` invariant. Only test futures that intentionally retain a lock across suspension now use `OwnedMutexGuard`.
- Restored deterministic LabRuntime contention in `mutex_contention_under_lab_runtime`: the holder yields with an owned guard until an atomic flag is set only after the waiter has actually polled its lock future to `Pending`.
- Strict remote compile proof passed: `cargo check -p asupersync --tests --features test-internals` with `-D warnings` on `vmi1152480`.
- Focused strict RCH proof passed in job `j-29927928582111234`: `cargo test -p asupersync --lib --features test-internals mutex_contention_under_lab_runtime -- --nocapture` (`1 passed; 0 failed`). The remote `src/sync/mutex.rs` SHA-256 `7bc6f80159d29e1fc77a590b712627579efc7ec0a45a8a72ab3fc43b40605693` matched the committed source before, during, and after the build.
- The reopened correctness bead is closed. Pass 4 may now re-evaluate the mailbox default, but it must rerun behavioral gates on the repaired source before retaining any config change.

### Pass 4 — mailbox-default correctness rerun, rejected on API semantics

- Fresh agent: `extreme_pass4_default_correctness`; completed 2026-07-12 with no retained repository source change.
- Base source: `174ef2070`. The isolated candidate changed only `src/runtime/config.rs` and `src/runtime/builder.rs`: it made `SpawnAdmissionMode::Mailbox` the default, updated its docs, asserted the config default, and made an existing real `RuntimeHandle` spawn/join test exercise that default.
- Candidate SHA-256 values were continuously checked locally and remotely: config `2f546aaa09c82e4d13ee8377f14aaba25c376512a90a48b30aedd6a926346c45`; builder `e6e2d7fb5d5396b6be28731421de4e73a7cb3c5d9a39a31b832538ce53b806b3`.
- Focused strict-RCH gates passed on the exact candidate: default assertion `j-29927928582111235` (1 test), default runtime spawn/join `j-29927928582111236` (1), the complete spawn-mailbox module `j-29927928582111237` (53), parker lost-wakeup stress `j-29927928582111238` (1), and the correctly invoked ignored inject-while-parking stress `j-29928343348445186` (1).
- Independent adversarial source review found a decisive public-contract failure before admission. `RuntimeHandle::try_spawn` and `try_spawn_with_cx` promise synchronous typed admission errors; default runtimes with root `max_tasks = 0` are required by `tests/builder_verification.rs` to return `Err(SpawnError::RegionAtCapacity)`. Mailbox mode instead enqueues and returns success before scheduler admission, deferring rejection into the join/cancel path.
- The same synchronous error contract drives HTTP/1 connection retry/guard release and HTTP/2 stream reset plus accepted-connection retry/drop branches. Default Mailbox would bypass those immediate typed-error paths. Review also found that mailbox drain injects directly into the global queue instead of the governor-aware checked injection path, so governor-enabled presets would silently bypass queue-limit warning, throttle, scheduler evidence, and wake-state accounting. Because enqueue is unbounded and hard task quotas are enforced only during later batched admission, the default would also stop a root task quota from bounding producer-side queued futures. Finally, `spawn_admission` is absent from config diffs/digests, so signed configs could identify Direct and Mailbox behavior as the same configuration.
- Fail-closed decision: reject the default flip. The final broad job `j-29928371131514883` was gracefully cancelled before tests began once the semantic blocker was confirmed (exit 130, deterministic cleanup complete, worker healthy); it supplies no test result. Strict Clippy was not started, and no broad-health claim is made.
- Root manually restored both candidate files with `apply_patch`. The restored SHA-256 values exactly match `HEAD`: config `4058034fd1a594171e3ad3220cf3d0628c0c43959ff21185ebc2a9287e8bcf84`; builder `62be8fc17fd181fccb2c6e1a0a2f6bd9b684a827f2de2fe5fd13b9ea8df25e65`. The scoped diff is clean and `Direct` remains the default.
- Reconsidering default promotion requires a separate design that preserves synchronous capacity/error semantics with a race-safe pre-enqueue permit, transfers that permit at admission, releases it on cancellation/close, and routes admitted work through the governor-aware injection path. That is not a one-lever default change and was intentionally not folded into this pass.
- Correctness interlock before Pass 5: repair and remotely prove the three confirmed peer regressions tracked as `asupersync-r2bpso`, `asupersync-0a03c5`, and `asupersync-pchkaf` before launching the next fresh optimization agent.

### Between Pass 4 and Pass 5 — peer regressions repaired and converged

- Static-file path hardening landed in `b371afc54` (`br-asupersync-r2bpso`): raw and recursively decoded backslashes are rejected before platform path construction, while canonical slash `.well-known` remains allowed. Strict RCH job `j-29928371131514884` passed all 66 static-file tests with `-D warnings`; exact source SHA-256 `7e0e211607df51b4ef85752b1ac5680f1ad2e79dcfd58fa6a659d42b865fd985`.
- Scheduler deadline accounting landed in `be5f2e1c9` (`br-asupersync-0a03c5`): four terminal completion paths now use `TaskTable::update_task`, preserving incremental deadline-pressure subtraction without double accounting. Strict RCH job `j-29928371131514885` passed the focused Worker slice; the same exact remote test binary passed the no-double-subtract regression and the scheduler namespace (655 passed, 0 failed, 5 ignored).
- Circuit-breaker Open publication landed in `0a71072b5` (`br-asupersync-pchkaf`): a private monotonic generation is independent of successful-open metrics, and one strong-CAS helper centralizes all three Open transitions so losing contenders cannot mutate metrics, windows, or callbacks. Exact source SHA-256 `02da8f0877ffaf682db07df160682f02fcf1a3412eb663c9d43c1a496eb1e125`; strict RCH job `j-29928371131514887` passed 45/45 focused tests and `j-29928371131514888` passed the full combinator namespace (711 passed, 0 failed, 1 ignored), all with `-D warnings`.
- Independent read-only reviews found no must-fix issue in any of the three repaired slices. Pinned nightly rustfmt checks and path-limited `git diff --check` passed; no local Cargo fallback was used.
- The shared convergence snapshot had RCH source hash `4c34c06124da65b7` for both commands. Strict remote job `j-29928371131514889` passed `cargo check --all-targets` with `-D warnings`; job `j-29928371131514890` passed `cargo clippy --all-targets -- -D warnings`. Both used `vmi1156319`, one build slot, and no local fallback.
- `main`, `origin/main`, and the legacy mirror all resolve to `0a71072b5c309c5210e4a9a62ebeeb873145b069`. The only remaining tracked worktree modification is peer-owned `src/audit/ambient.rs`; it is not staged or altered by this campaign.

### Pass 5 — spawn-mailbox admission batch sensitivity, batch 1 retained

- Fresh agent: `extreme_pass5_batch_sensitivity`; exact-final source SHA-256 `ccd30349b8b6ea554baff1610167650c5c6faedb00e0c5eb76e9249fc14bd579`, diff SHA-256 `b0a932d4b9c1b64abd38730e2c1bd471c538f5294695448fd17d6090ce14dbfd`. The retained scheduler change reduces `SPAWN_ADMISSION_BATCH` from 16 to 1.
- Experiment: explicit mailbox mode, persistent eight-producer corrected workload, 1,000 admitted spawns/iteration, 3 s warmup, 8 s measurement, 100 flat samples, one worker (`vmi1156319`), pinned nightly, order `16 -> 1 -> 4 -> 64 -> 64 -> 4 -> 1 -> 16`. Contaminated attempts were quarantined and no peer process was canceled.

| Batch/order | Raw mean ms | Throughput Kelem/s | Raw p50 ms | Raw p95 ms | Raw p99 ms |
|---|---:|---:|---:|---:|---:|
| 16 forward | 21.690 | 46.103 | 20.782 | 28.245 | 32.111 |
| 1 forward | 17.061 | 58.614 | 16.290 | 22.585 | 29.185 |
| 4 forward | 26.273 | 38.062 | 24.923 | 35.087 | 48.524 |
| 64 forward | 20.019 | 49.954 | 19.909 | 23.232 | 24.973 |
| 64 reverse | 20.005 | 49.987 | 19.884 | 24.025 | 25.776 |
| 4 reverse | 23.766 | 42.078 | 24.002 | 28.378 | 30.707 |
| 1 reverse | 17.423 | 57.397 | 17.306 | 21.807 | 26.616 |
| 16 reverse | 21.376 | 46.780 | 20.910 | 25.769 | 40.007 |

- The original matched matrix favored batch 1 over batch 16 by `+27.135%` and `+22.694%` throughput (`+24.895%` geometric), with lower p95/p99 in both orderings. Batch 64 improved throughput modestly but was dominated by batch 1; batch 4 regressed.
- Mechanism: batch 16 holds the shared `RuntimeState` lock across sixteen admissions. Batch 1 pays more acquisitions but interleaves admission with execution/completion and sharply shortens each convoy-producing critical section on this workload.
- Adversarial review found a real denied-backlog regression in the initial one-line candidate: after one denial produced no runnable task, the I/O leader could block for the 250 ms idle timeout while mailbox requests remained. The final source adds `select_io_poll_timeout`, makes pending mailbox work force a nonblocking poll, and covers all four fast-queue/mailbox decision combinations. This repair is outside the all-admitted benchmark hot path.
- Exact-final performance was rerun from a provenance-bound binary (recorded SHA-256 `20b19e37433f5330855b63cb94b450181729104aa35cb557630f728babf3da40`) with three clean preflight probes and sampled in-window peer-process monitoring. Replicate means were `17.247548738 ms` (`57.979 Kelem/s`) and `16.575808080 ms` (`60.329 Kelem/s`). Against the clean opening/closing batch-16 anchors, matched throughput gains were `+25.759%` and `+28.962%`, or `1.273505x` geometric (`21.477%` lower geometric mean time). Both p95s improved; one p99 improved `43.651%`, while the other was `2.812%` higher and remained inside the 10% tail guard.
- Exact-final evidence manifest: `/data/tmp/asupersync_pass5_spawn_batch_20260712/exact_final/FINAL_SOURCE_MANIFEST.md`, SHA-256 `564551732e41b48ea5112ee716a12f0374852c8e85bf634aabc7f6019209d7fc`. Independent review rehashed every accepted/rejected artifact, recomputed all statistics, verified the diff and remote jobs, and returned `CLEAN TO COMMIT`.
- Strict remote focused proof: scheduler job `j-29928812120637451` passed 200 tests with 5 ignored and 0 failed; spawn-mailbox job `j-29928812120637452` passed 53/53. Both used the exact final source, jobs=1, `-D warnings`, and no local fallback.
- Broad convergence: `cargo check --all-targets` passed under strict remote RCH job `j-29928812120637455`; `cargo clippy --all-targets -- -D warnings` passed under job `j-29928812120637456`. The first check control session (`j-29928812120637453`) hit the default 1,200 s SSH timeout while its owned remote Cargo process continued; an idle duplicate waiter (`j-29928812120637454`) was canceled, then job 455 used the correct 7,200 s timeout, waited for the owned target lock, and returned the green result. No peer build was canceled and no local Cargo fallback was used.
- Benchmark harness restoration is byte-exact at SHA-256 `6d096cf4400cdc6f86c1a22437128db0044b9e09308150cb90bad407d8812a15`. Pinned nightly rustfmt, path-limited `git diff --check`, and a path-limited UBS scan passed.
- Limits: monitoring sampled the binary interval rather than continuously (48/49 clean probes, maximum gaps about 357/314 ms); the recorded benchmark binary was later removed with its ephemeral RCH target, so its capture receipt and remote build job are the surviving provenance; the opening batch-16 anchor had weaker interval monitoring than the exact-final replicates. These measurements cover admitted batch throughput and per-iteration average tails, not individual-request latency, denial/close storms, sustained queue depth, scheduler-wide throughput, or `drive_io_phase` performance equivalence. Batch 1 also increases lock-acquisition count, and the owner-local spawn lane has a separate pre-existing denial-backlog shape outside this pass.
