# Stub/Placeholder Allowlist
# Intentional exceptions for the stub-resolution scan ratchet.
# Format: path:symbol (reason) [disposition]
#
# These surfaces are NOT bugs — they are honest, documented, and tested.
# `scripts/scan_stubs.sh` validates that each entry parses, points at a live
# path, and still matches a symbol present in that file.

# Platform-conditional reactors that return Err(Unsupported) on unsupported platforms
# Note: kqueue.rs and windows.rs are cfg-gated at the module level (only compiled on
# their target platform), so they have no cfg-off stub to waive. Only io_uring.rs
# exports a type on Linux that returns Unsupported without the io-uring feature.
src/runtime/reactor/io_uring.rs:IoUringReactor (Linux export; without io-uring feature returns Err(Unsupported)) [DOCUMENT]

# Feature-gated harness doubles
src/transport/mock.rs:SimNetwork (cfg(any(test, feature = "test-internals")) test double) [QUARANTINE]

# TLS feature-gated stubs that return proper "feature not enabled" errors
src/tls/types.rs:Certificate::from_pem (cfg(not(feature = "tls")) returns feature error) [DOCUMENT]
src/tls/types.rs:PrivateKey::from_pem (cfg(not(feature = "tls")) returns feature error) [DOCUMENT]

# Historical design document (moved to docs/design/)
docs/design/api_skeleton_v4.rs:skeleton_placeholder! (design reference, not compiled) [RETIRE]
